name: ethioaiventure
version: 1.0.1
description: "Ethiopian & African AI venture intelligence agent — ecosystem research, startup validation, opportunity scoring, and scheduled intelligence reports (Telegram delivery)."
hermes_requires: ">=0.20.0"
author: "tcyber925-del"
license: "MIT"
# Installers bring their own model credentials. For the Nous Portal provider
# (default in config.yaml), run: hermes setup --portal  (or hermes auth)
env_requires: []